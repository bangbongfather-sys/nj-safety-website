/**
 * Canonical production URL — used as metadataBase so Open Graph / Twitter
 * card image + url values resolve to absolute URLs for link-preview
 * crawlers (KakaoTalk, iMessage, Slack, etc.).
 *
 * This is the domain the site is actually served from today. When a custom
 * domain (e.g. https://www.njfashion.co.kr) is pointed at the Worker, switch
 * this one line and redeploy.
 */
export const SITE_URL = 'https://nj-safety-website.njsafety91.workers.dev';
